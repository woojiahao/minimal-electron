import React from 'react';
import ReactDOM from 'react-dom';
// import entry component
import Entry from './Entry.jsx';


// render `Entry` component
ReactDOM.render(React.createElement(Entry), document.getElementById('app'));